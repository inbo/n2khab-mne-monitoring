+ [[ElevationPoints input 3D coordinates diractly via qfield]] more testing & rewrite to capture altitude from QField `@position_coordinates`
+ remove `type` from #TargetPoints since they are type-independent
	+ ```sql
    ALTER TABLE "transfer"."TargetPoints" DROP COLUMN "type";
    ```
	+ made sure this is reflected in QGIS
+ removed test location in Neeltje-Jans
+ briefly inspected #ExtraLocevals nice effort by #WT

+ icons to be embedded in qgis projects [[symbols for FreeFieldNotes layer symbology|FreeFieldNotes symbols]]
