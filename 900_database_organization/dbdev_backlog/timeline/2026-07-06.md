+ re-run dailies
+ refine #mnmsurfdb #QGIS projects:
	+ add #SamplingPoints to fieldwork
	+ add `*Observations` to planning
	+ extra fields: 
	+ ```sql
	  ALTER TABLE "inbound"."GeoObservations" ADD COLUMN depth_class varchar; 
      COMMENT ON COLUMN "inbound"."GeoObservations".depth_class IS E'depth class of the water (1.5m, 3m, 6m?)';

      ALTER TABLE "inbound"."GeoObservations" ADD COLUMN bank_slopeclass varchar; 
      COMMENT ON COLUMN "inbound"."GeoObservations".bank_slopeclass IS E'bank slope class (determined by depth at 1m distance from edge)';
      
	  ```
+ why no delete #Observations ?
	+ it works when directly applied to `Observations`
	+ probably an issue with inheritance
	+ [[qgis/features of inherited tables cannot be deleted via QGIS]]
+ [[why does loceval info not arrive in the mnmsurfdb planning layer]]?