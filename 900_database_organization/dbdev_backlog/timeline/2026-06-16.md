- [[users/introduce a new read-only user for an adjacent team|introduce a new read-only user for an adjacent team]]
- brief overview of the database structure (inkscape sketch because `pgmodeler` got commercial)
- #locevaldb #Replacements are missing for locations, e.g. `1466998`
	- ```sql
	  SELECT * FROM "outbound"."Replacements" WHERE grts_address = 1466998;
	  ```
	- issue was wrong join to `REP::grts_address` instead of `grts_address_final` (our #SampleUnits use the final address, thus we must join #REP data by their `_final`)
	- partial REP update performed 
		- `outbound.Replacements` -> now 38699 (were 38292) replacement candidates
		- also updated #ReplacementArchives and #ReplacementCells (triv.)
