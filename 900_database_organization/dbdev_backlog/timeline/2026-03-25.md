- fixed #ReplacementOngoing : [[views/replacement locations visible in QGIS but not in ReplacementOngoing]]
- added function to #snippets :
	- `extract_and_flatten_scheme_from_scheme_ps_targetpanels()` to extract `schemes` from flattened `scheme_ps_targetpanels`
	- `check_presence_of_required_library("stringr")()` to centralize library / namespace checks
- continued [[data update/20260313 REP update 0.15.0|REP 0.15.0]] for #mnmgwdb 
	- allow `NA`->`NULL` values in #FieldworkCalendar `priority`: 
	```sql
    ALTER TABLE "outbound"."FieldActivityCalendar" ALTER COLUMN priority DROP NOT NULL;
    ALTER TABLE "outbound"."FieldworkCalendar" ALTER COLUMN priority DROP NOT NULL;
	```
	- first successful creep-through of #mnmgwdb `610_mnmgwdb_update_REP.qmd`; more to check