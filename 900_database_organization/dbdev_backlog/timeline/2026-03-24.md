- another [[database connection mirror falk-up]]?! I think this time cause of actions was as follows: 
	- this had happened before in previous falk-ups, but without me noticing
	- I was working on `-staging` with `510_loceval_update_REP.qmd` 
	- while, most likely, a subprocess (script) connected to `production` (actually I don't know how it happened)
	- suddenly, I saw attribute distribution printed which was not plausible; `db$shellstring` revealed that R had switched mirror
	- this was just after switching `405_freeze_calendar.R` to a real login; might have to do with R environments and a db in a global env
	- [[database connection mirror falk-up|production protection prompt]] introduced
- [[data update/20260313 REP update 0.15.0|REP 0.15.0]] continued
	- finished the script; most relevantly: #FieldworkCalendar and Replacements
	- first attempt on `staging` seemed promising
	- first attempt `production` ran through, but then I saw weird #activities on #QGIS
		- those turn out to be harmless
		- [[remove archived non-loceval activities from locevaldb]]
		- however, the #view for #FieldworkPlanning should be tweaked (also excluding `is_frozen` unless `done_planning`)
	- success (almost): `locevaldb` is up to date
		- BUT: `SOIL` locations not included yet -> issue with actual `SampleUnits.archive_version_id` must have been archived before
		- RESTORED: 
		  ```sql
		  UPDATE
           "outbound"."SampleUnits"
         SET archive_version_id = NULL
         WHERE sampleunit_id IN (
           SELECT DISTINCT sampleunit_id
           FROM "outbound"."FieldActivityCalendar"
           WHERE archive_version_id IS NULL
         )
         AND archive_version_id IS NOT NULL
         ;
		  ```
- [x] [[issue with all checksums changing after R update]]
- [[replacement locations visible in QGIS but not in ReplacementOngoing]]