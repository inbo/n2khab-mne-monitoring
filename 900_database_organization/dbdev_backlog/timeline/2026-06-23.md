- [[data update/20260619 REP preview 0.17.0|REP 0.17.0]] -> `schemes_served_all` consolidation
- add info about `fag_is_auxiliary` and `fag_is_preponable` (see [[structure/flag auxiliary FAGs|flag auxiliary FAGs]], [[memos/field activity calendar date linking adjustments on REP update prepone prepend|field activity calendar date linking adjustments on REP update prepone prepend]])
```sql
SELECT DISTINCT activity_group, activity, fag_is_auxiliary, fag_is_preponable, archive_version_id FROM "metadata"."GroupedActivities";
UPDATE "metadata"."GroupedActivities"
SET fag_is_auxiliary = FALSE
WHERE fag_is_auxiliary IS NULL
;
UPDATE "metadata"."GroupedActivities"
SET fag_is_preponable = FALSE
WHERE fag_is_preponable IS NULL
;
```
- open questions #FV about:
	- `schemes_served_all` -> on level of #SampleUnits
	- safety trimming in `location_attribute_processing.R/nest_and_flatten_scheme_ps_targetpanel`
	- handling of `"SURFLENTSECC", "SURFLOTSECC"`