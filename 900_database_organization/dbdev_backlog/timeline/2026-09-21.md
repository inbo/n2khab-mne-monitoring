## continue

+ [[implement optional and intrinsic matching occasions in all databases]]
+ [[test array data types connection to R]]

## ad hoc data hygiene

### consistency `excluded` and `no_visit_planned`
#### #locevaldb obsolidate `no_visit_planned`

```sql
SELECT DISTINCT excluded, no_visit_planned, is_frozen, COUNT(*) AS n
FROM "outbound"."FieldCalendars"
GROUP BY excluded, no_visit_planned, is_frozen
;

-- UPDATE "outbound"."FieldCalendars"
-- SET excluded = TRUE, excluded_reason = (CASE WHEN excluded_reason IS NULL THEN '' ELSE excluded_reason || ' ' END || '[was nvp FM 20260921]')
-- WHERE (NOT excluded) AND (no_visit_planned)
-- ; -- done


```

#### #mnmgwdb obsolidate `no_visit_planned`

```sql
SELECT DISTINCT excluded, no_visit_planned, is_frozen, COUNT(*) AS n
FROM "outbound"."FieldCalendars"
GROUP BY excluded, no_visit_planned, is_frozen
;

 excluded | no_visit_planned | is_frozen |  n
----------+------------------+-----------+------
 f        | f                | f         | 9304
 f        | f                | t         |  192
 f        | t                | t         | 1364
 t        | f                | f         |  326
 t        | f                | t         |    5 okay
 t        | t                | f         |    7 okay
 t        | t                | t         |   99 okay
(7 rows)

```


```sql
-- those which are `nvp` but neither excluded, nor frozen
SELECT *
FROM "outbound"."FieldCalendars"
WHERE  (NOT excluded) AND (no_visit_planned) AND (NOT is_frozen)
; -- GONE (were remnants on `-staging`)
```


```sql
-- those which are `nvp` AND frozen, but not excluded
SELECT *
FROM "outbound"."FieldCalendars"
WHERE  (NOT excluded) AND (no_visit_planned) AND (is_frozen)
AND log_user != 'admin';
; -- manually set all to `excluded` in QGIS
```

```sql
SELECT *
FROM "outbound"."FieldCalendars"
-- UPDATE "outbound"."FieldCalendars"
-- SET excluded = TRUE, notes = (CASE WHEN notes IS NULL THEN '' ELSE notes END || ' [was nvp FM 20260921]')
WHERE  (NOT excluded) AND (no_visit_planned) AND (is_frozen)
AND log_user = 'admin'
; -- done
```

two had an `archive_version_id`

```sql
UPDATE "outbound"."FieldCalendars"
SET excluded = TRUE, 
  notes = (CASE WHEN notes IS NULL THEN '' ELSE notes END || ' [was nvp FM 20260921]') -- should have used `excluded_reason`
WHERE (NOT excluded) AND (no_visit_planned) AND (is_frozen)
AND archive_version_id IS NOT NULL
; -- done

-- correctly place notes
UPDATE "outbound"."FieldCalendars"
SET excluded_reason = (CASE WHEN excluded_reason IS NULL THEN '' ELSE excluded_reason || ' ' END || ' [nvp by FM 20260410]'),
 notes = REPLACE(notes, ' [nvp by FM 20260410]', '')
WHERE notes LIKE '%nvp by FM 20260410]'
; -- done

UPDATE "outbound"."FieldCalendars"
SET excluded_reason = (CASE WHEN excluded_reason IS NULL THEN '' ELSE excluded_reason || ' ' END || ' [was nvp FM 20260921]'),
 notes = REPLACE(notes, ' [was nvp FM 20260921]', '')
WHERE notes LIKE '%was nvp FM 20260921]'
; -- done



```

#### #mnmsurfdb obsolidate `no_visit_planned`

```sql
SELECT DISTINCT excluded, no_visit_planned, is_frozen, COUNT(*) AS n
FROM "outbound"."FieldCalendars"
GROUP BY excluded, no_visit_planned, is_frozen
;

SELECT grts_address, stratum, date_start, excluded_reason, notes
FROM "outbound"."FieldCalendars"
WHERE  (excluded OR no_visit_planned)
;


SELECT grts_address, stratum, date_start, excluded_reason, notes
FROM "outbound"."FieldCalendars"
WHERE  (NOT excluded) AND (no_visit_planned)
  AND (notes LIKE '%droog%' OR notes LIKE 'Droog%')
   OR notes LIKE '%geen loceval%'
;


UPDATE "outbound"."FieldCalendars"
SET excluded_reason = (CASE WHEN excluded_reason IS NULL THEN '' ELSE excluded_reason || ' ' END || '[geen loceval]')
WHERE ((excluded) OR (no_visit_planned))
  AND notes LIKE '%geen loceval%'
; -- done / 23

UPDATE "outbound"."FieldCalendars"
SET excluded_reason = (CASE WHEN excluded_reason IS NULL THEN '' ELSE excluded_reason || ' ' END || '[droogval]')
WHERE ((excluded) OR (no_visit_planned))
  AND (notes LIKE '%droog%' OR notes LIKE 'Droog%')
; -- done / 17

UPDATE "outbound"."FieldCalendars"
SET excluded = TRUE, excluded_reason = (CASE WHEN excluded_reason IS NULL THEN '' ELSE excluded_reason || ' ' END || '[was nvp FM 20260921]')
WHERE (NOT excluded) AND (no_visit_planned)
; -- done / 40

```


## remove priorities from frozen activities

on #mnmgwdb and #locevaldb there were still frozen units with priorities.


```sql

SELECT DISTINCT priority, COUNT(*) AS n
FROM "outbound"."FieldCalendars"
WHERE is_frozen 
GROUP BY priority
;
-- UPDATE "outbound"."FieldCalendars" 
-- SET priority = NULL
-- WHERE is_frozen 
--   AND priority IS NOT NULL
-- ;

```