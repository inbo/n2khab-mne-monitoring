- [[consistent table and field names across databases]] ctd.
	- [[rename SampleLocations to SampleUnits in mnmgwdb]]
	- [[rename FieldworkCalendar to FieldCalendars in mnmgwdb]]

- locations with issues (request #TDD via chat) and the #LocationJournals way of determining `has_installation`.
```sql
SELECT grts_address, stratum, date_visit, issues, visit_done, notes FROM "inbound"."InstallationVisits" WHERE grts_address IN (47685766, 1904953, 15026297, 4211622, 1904953, 10745, 53288370, 7151026);
```

- `log_user` and `log_update` were not correctly filled in #SamplingVisits and #PositioningVisits of #mnmgwdb 
	- Due to mistyping the `DROP` prerequisite in the structure tables.


- #reporting we figured that counting #InstallationVisits could be misleading due to adoption of existing #WATINA locations
	- in those cases, `visit_done` is TRUE,
	- #TDD can identify them based on WATINA code