+ [[data update/20260819 REP update 0.18.0]]
+ adjustments on #FieldFollowUps on #mnmsurfdb to capture extra chlorophyll measurement locations
	+ ```sql
    ALTER TABLE "inbound"."FieldFollowUps" ADD COLUMN chlorophyll_extra boolean DEFAULT NULL; 
      COMMENT ON COLUMN "inbound"."FieldFollowUps".chlorophyll_extra IS E'extra location (or not) for Chlorophyll test measurements';
    -- ALTER TABLE "inbound"."FieldFollowUps" DROP COLUMN replacement;
    -- ALTER TABLE "inbound"."FieldFollowUps" DROP COLUMN repair;
    ```
+ wrong #QGIS datatype for #ChlorophyllMeasurements `photo` led to dysfunction of that field (no camera); fixed and re-uploaded