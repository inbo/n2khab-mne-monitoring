+ check which #loceval #Visits were registered
	+ ```sql
	 SELECT DISTINCT
       teammember_id,
       type,
       type_assessed,
       count(*) AS n
     FROM "inbound"."Visits"
     WHERE visit_done
     AND NOT issues
     GROUP BY
       teammember_id,
       type,
       type_assessed
     ORDER BY n DESC
     ;
	  ```
+ issue: #RandomPlacementPoints were missing for duo-cells (7150+4010) which received a loceval for 7150, but are not on the agenda for 4010 (receiving 4010 cellmap nonetheless)