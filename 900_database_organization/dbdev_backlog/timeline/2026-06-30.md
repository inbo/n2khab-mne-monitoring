+ [[users/new users and extra roles for initialization of SURF scheme]]
+ continue re-init #mnmsurfdb
	+ for the moment, only `*DATACOLL` activities are associated with SURF
+ issues with #FreeFieldNotes `110_sync_FreeFieldNotes.R` and #mnmsurfdb sync:
	+  ```sql
      BEGIN;
        ALTER TABLE "inbound"."FreeFieldNotes" ALTER COLUMN "log_origindb" TYPE varchar(16);
      COMMIT;
      ```
	+ existing notes double-upload due to join mismatch -> time was shifted by two hours
		this had never been an issue before!?
		-> issue was timezone in `strftime(ts, format = "%Y-%m-%d %H:%M:%OS0", tz = "UTC")` in `convert_timestamp_to_ms_character`