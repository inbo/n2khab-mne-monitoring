+ [[implement optional and intrinsic matching occasions in all databases]]
+ Chat #WT #WD will now use the `type_suggested` on #Replacements 
	+ naming was confusing, is now `type (vastgesteld, vervangcel)`
	+ ```sql
	  loceval=# UPDATE "outbound"."Replacements" SET type_suggested = type WHERE is_selected;
      UPDATE 158
	  ```